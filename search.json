[{"title":"LeetCode 刷题笔记","url":"/2026/04/18/LeetCode/","content":"Python常用数据结构与算法基本数据结构list - 列表\n\n\n函数&#x2F;方法\n作用\n时间复杂度\n刷题场景举例\n\n\n\nlen(lst)\n获取列表长度\n\n滑动窗口（判断窗口大小）、数组遍历\n\n\nlst.append(x)\n尾部添加元素\n\n结果收集（如滑动窗口最大值存入ans）\n\n\nlst.pop()\n尾部弹出元素\n$O(1)$\n栈&#x2F;队列模拟（默认弹最后，pop(0)是O(n)）\n\n\nlst.pop(i)\n弹出指定下标元素\n\n单调队列（移除窗口外元素）\n\n\nlst.sort()\n原地排序（Timsort）\n$O(n log n)$\n数组排序、TopK问题预处理\n\n\nlst.reverse()\n原地反转列表\n\n回文判断、反转数组\n\n\nlst.index(x)\n查找元素x的第一个下标（无则报错）\n\n找元素位置、子数组匹配\n\n\nlst.count(x)\n统计元素x出现次数\n\n频次统计、多数元素问题\n\n\nlst[:]\n列表切片（浅拷贝）\n\n子数组&#x2F;子串提取（如最小覆盖子串结果）\n\n\nlst.extend(iter)\n批量添加可迭代对象元素\n\n合并多个列表\n\n\n二维数组按照首元素排序\nnums.sort(key=lambda x:x[0])\n对前k个元素反转列表\nnums, k = [1, 2, 3, 4, 5], 3# 错误示范# 1. nums[:k]: 创建 nums 从索引 0 到 k-1 的一个新列表。# 2. .reverse(): 调用这个新列表的 reverse 方法，将其反转。nums[:k].reverse()# 应该采用下面的方法nums[:k] = nums[:k][::-1]\n\n二维数组列交换\n# 错误示范for j in range(m//2)\tmatrix[:][j], matrix[:][m-1-j] = matrix[:][m-1-j], matrix[:][j]# 应当采用for i in range(m):\tfor j in range(m//2):\t\tmatrix[i][j], matrix[i][m-1-j] = matrix[i][m-1-j], matrix[i][j]\n\n\n\nstr - 字符串\n\n\n函数&#x2F;方法\n作用\n刷题场景举例\n\n\n\nlen(s)\n获取字符串长度\n字符串遍历、子串长度判断\n\n\ns[i]\n按下标访问字符\n双指针（回文、滑动窗口）\n\n\ns.split(sep)\n按分隔符分割字符串\n解析字符串格式（如”1,2,3”转列表）\n\n\ns.join(iter)\n拼接可迭代对象为字符串\n结果拼接（如列表转字符串返回）\n\n\ns.strip()\n去除首尾空白&#x2F;指定字符\n字符串预处理（如去除无用空格）\n\n\ns.replace(old, new)\n替换子串\n字符串修改、模式匹配\n\n\ns.find(sub)\n查找子串下标（无则返回-1）\n子串存在性判断、滑动窗口匹配\n\n\ns.isdigit()\n判断是否全为数字\n数值字符串校验\n\n\ns.lower()/s.upper()\n大小写转换\n忽略大小写的字符串比较\n\n\ndict - 字典注意边界：如dict.get(key, 0)避免KeyError。\n\n\n\n函数&#x2F;方法\n作用\n刷题场景举例\n\n\n\nd[key]\n访问&#x2F;赋值键对应的值（无则报错）\n频次统计、两数之和（存值-下标映射）\n\n\nd.get(key, default)\n访问键值（无则返回default）\n避免KeyError，频次统计（默认0）\n\n\nd.keys()\n获取所有键\n遍历字典键（如判断字符是否在t中）\n\n\nd.values()\n获取所有值\n频次统计后取值\n\n\nd.items()\n获取键值对（(k,v)）\n遍历字典（如排序字典、筛选频次）\n\n\nd.pop(key)\n删除键并返回值\n移除无用的键值对\n\n\ndel d[key]\n删除键值对\n同pop，无返回值\n\n\nset - 集合\n\n\n函数&#x2F;方法\n作用\n刷题场景举例\n\n\n\nset()\n创建集合，常用于去重\n数组&#x2F;字符串去重 (list(set(nums)))\n\n\nlen(s)\n获取集合长度\n统计不重复元素个数\n\n\ns.add(x)\n添加元素到集合\n记忆化搜索、哈希表模拟 (visited.add(node))\n\n\ns.remove(x)\n从集合移除元素（不存在则报错）\n滑动窗口收缩（移除左边界元素）\n\n\ns.discard(x)\n从集合移除元素（不存在不报错）\n安全地移除元素，避免 KeyError\n\n\nx in s\n检查元素是否在集合中\n快速查找&#x2F;存在性判断 (if node in visited:)\n\n\ncollections 库defaultdict - 带默认值的字典from collections import defaultdictd = defaultdict(int)  # 默认值为0，频次统计首选d = defaultdict(list) # 默认值为列表，存多值映射（如字母对应下标列表）\n\n\n\n\n核心用法\n刷题场景举例\n\n\n\nd[key] += 1\n字符&#x2F;数字频次统计（最小覆盖子串、多数元素）\n\n\nd[key].append(x)\n分组（如数组元素按值分组、图的邻接表）\n\n\ndeque - 双端队列注意时空复杂度：比如列表pop(0)是$O(n)$，刷题时优先用$O(1)$的deque.popleft()。\nfrom collections import dequeq = deque()\n\n\n\n\n函数&#x2F;方法\n作用\n时间复杂度\n刷题场景举例\n\n\n\nq.append(x)\n尾部添加元素\n\n队列&#x2F;栈模拟\n\n\nq.appendleft(x)\n头部添加元素\n\n双端队列专用（如单调队列）\n\n\nq.pop()\n尾部弹出元素\n\n栈模拟（后进先出）\n\n\nq.popleft()\n头部弹出元素\n$O(1)$\n队列模拟、滑动窗口（单调队列移除左边界）\n\n\nq[0]\n访问队首元素\n\n单调队列取窗口最大值\n\n\nq[-1]\n访问队尾元素\n\n单调队列判断尾部元素是否需要弹出\n\n\nCounter - 频次统计from collections import Countercnt = Counter(iter)  # 从可迭代对象初始化频次字典\n\n\n\n\n函数&#x2F;方法\n作用\n刷题场景举例\n\n\n\ncnt[x]\n获取&#x2F;修改元素x的频次\n同defaultdict(int)，更简洁\n\n\ncnt.most_common(k)\n获取前k个高频元素（列表，(元素,频次)）\nTopK问题（如前k个高频数字）\n\n\ncnt1 == cnt2\n判断频次是否完全一致\n异位词判断（如有效的字母异位词）\n\n\ncnt1.subtract(cnt2)\n频次相减\n最小覆盖子串（判断窗口是否满足）\n\n\nheapq 库 - 堆&#x2F;优先队列Python 堆默认是小根堆，大根堆需通过“值取负”实现，是 TopK、滑动窗口最大值等问题的核心工具。\nimport heapqheap = []\n\n\n\n\n函数&#x2F;方法\n作用\n时间复杂度\n刷题场景举例\n\n\n\nheapq.heappush(heap, x)\n入堆（维护堆结构）\n\n堆排序、滑动窗口最大值（存-值+下标）\n\n\nheapq.heappop(heap)\n出堆（弹出堆顶，最小元素）\n\n移除堆顶无效元素（如窗口外的最大值）\n\n\nheap[0]\n访问堆顶元素（不弹出）\n\n获取当前堆的最小值&#x2F;最大值（取负后）\n\n\nheapq.heapify(lst)\n原地将列表转为堆\n$O(n)$\n初始化堆（如滑动窗口前k个元素建堆）\n\n\nheapq.nlargest(k, lst)\n获取列表中前k个最大元素\n$O(n log k)$\nTopK问题（比排序更高效）\n\n\nheapq.nsmallest(k, lst)\n获取列表中前k个最小元素\n\n最小k个数问题\n\n\nmath 库\n\n\n函数\n作用\n刷题场景举例\n\n\n\nmath.abs(x)\n绝对值\n数值比较、距离计算\n\n\nmath.sqrt(x)\n平方根\n几何题、距离公式（如欧氏距离）\n\n\nmath.pow(x, y)\nx的y次方\n数值计算、幂次相关问题\n\n\nmath.gcd(a, b)\n最大公约数\n数论问题、分数化简\n\n\nmath.lcm(a, b)\n最小公倍数\n数论问题\n\n\nmath.inf\n无穷大\n初始化极值（如最小覆盖子串初始长度）\n\n\nmath.floor(x)\n向下取整\n数值处理、分桶问题\n\n\nmath.ceil(x)\n向上取整\n数值处理、需要整数结果的计算\n\n\nbisect 库 - 二分查找bisect仅适用于有序数组。\nimport bisectlst = [1,3,5,7]  # 必须是有序数组\n\n\n\n\n函数\n作用\n刷题场景举例\n\n\n\nbisect.bisect_left(lst, x)\n找x插入的最左位置（无则返回len(lst)）\n二分查找、有序数组去重、找边界\n\n\nbisect.bisect_right(lst, x)\n找x插入的最右位置\n统计x出现次数（right - left）\n\n\nbisect.insort_left(lst, x)\n插入x并保持有序\n维护有序列表（如数据流中位数）\n\n\nsortedcontainers 库 - 有序列表\n\n\n类别\n方法\n语法\n描述\n示例\n\n\n\n创建与初始化\n构造函数\nSortedList(iterable)\n创建一个新的有序列表。如果传入可迭代对象，则将其元素排序后放入列表。\nsl = SortedList([3, 1, 4]) 结果为 [1, 3, 4]\n\n\n添加元素\nadd()\nsl.add(value)\n将一个元素 value 添加到列表中，并保持列表的升序状态。\nsl = SortedList([1, 3]); sl.add(2) 结果为 [1, 2, 3]\n\n\n\nupdate()\nsl.update(iterable)\n将一个可迭代对象中的所有元素添加到列表中，并保持列表的升序状态。\nsl = SortedList([1]); sl.update([3, 2]) 结果为 [1, 2, 3]\n\n\n删除元素\nremove()\nsl.remove(value)\n删除列表中第一个等于 value 的元素。如果不存在则会引发 ValueError。\nsl = SortedList([1, 2, 3]); sl.remove(2) 结果为 [1, 3]\n\n\n\ndiscard()\nsl.discard(value)\n删除列表中第一个等于 value 的元素。如果不存在也不会引发错误。\nsl = SortedList([1, 3]); sl.discard(2) (无错误，列表不变)\n\n\n\npop()\nsl.pop(index)\n移除并返回指定索引 index 处的元素。默认为 -1 (最后一个元素)。\nsl = SortedList([1, 2, 3]); val = sl.pop() val 为 3\n\n\n\nclear()\nsl.clear()\n清空列表，使其为空。\nsl = SortedList([1, 2]); sl.clear() 结果为 []\n\n\nlambda# lambda 函数被赋值给变量 add_func，使其可以被调用add_func = lambda x, y: x + y\n\n位运算&#x2F;异或\n\n\n运算符\n名称\n符号\n核心规则（逐位运算）\n示例（十进制→二进制）\n常用场景\n\n\n\n左移\nShift Left\n&lt;&lt;\n所有位左移 n 位，右侧补 0，等价于 ×2ⁿ\n3&lt;&lt;2 → 12（011→1100）\n快速计算 2 的幂、生成定位符\n\n\n右移\nShift Right\n&gt;&gt;\n所有位右移 n 位，左侧补符号位，等价于 ÷2ⁿ（向下取整）\n12&gt;&gt;2 → 3（1100→0011）\n快速整除 2\n\n\n按位与\nAND\n&amp;\n对应位都为 1 则为 1，否则为 0\n5&amp;3 → 1（101&amp;011&#x3D;001）\n判断某一位是否为 1、掩码筛选\n\n\n按位或\nOR\n|\n对应位有 1 则为 1，否则为 0\n5|3 → 7（101|011&#x3D;111）\n置位（将某一位设为 1）\n\n\n按位异或\nXOR\n^\n对应位不同则为 1，相同则为 0\n5^3 → 6（101^011&#x3D;110）\n找不同、交换变量、消去重复数\n\n\n按位取反\nNOT\n~\n所有位取反（Python 是～x &#x3D; -(x+1)）\n~5 → -6（101→…11111010）\n快速取反、生成反掩码\n\n\n其他高频函数优先用内置函数：内置函数（如sorted、len）底层是C实现，比手动写循环快；\n\n\n\n函数\n作用\n刷题场景举例\n\n\n\nsorted(iter, key=..., reverse=...)\n通用排序（返回新列表）\n自定义排序（如按元组第二个元素、字典值排序）\n\n\nenumerate(iter, start=0)\n遍历可迭代对象，返回(下标, 元素)\n数组&#x2F;字符串遍历（需同时用下标和值）\n\n\nzip(iter1, iter2)\n打包多个可迭代对象为元组迭代器\n多数组同步遍历（如矩阵行和列）\n\n\nrange(start, end, step)\n生成整数序列\n循环遍历（如矩阵行&#x2F;列遍历）\n\n\nmax(iter)/min(iter)\n取可迭代对象的最大&#x2F;最小值\n极值问题（如滑动窗口最大值）\n\n\nsum(iter)\n求和\n子数组和、前缀和计算\n\n\nord(c)\n获取字符 c 的 Unicode 码点值\n字符哈希（如 arr[ord(c) - ord(&#39;a&#39;)]）、字符转数字\n\n\nchr(i)\n获取 Unicode 码点为 i 的字符（ord的反向）\n数字转字符、实现简单的加密&#x2F;解密、生成字母序列\n\n\n常见问题字符数组转字符串index = &#x27;&#x27;.join(sorted(s))\n\n读入数组nums = list(map(int, input().strip().split(&#x27;,&#x27;)))\n\n三元表达式if age &gt;= 18:    access = &quot;允许&quot;else:    access = &quot;禁止&quot;access = &quot;允许&quot; if age &gt;= 18 else &quot;禁止&quot;\n\n可变对象&#x2F;不可变对象\n\n\n类型\n定义\n举例\n\n\n\n不可变对象\n创建后自身值无法修改，若要 “修改”，本质是创建新对象并让变量指向新地址\nint、str、float、tuple、bool\n\n\n可变对象\n创建后自身值可以修改，修改时不会创建新对象，内存地址保持不变\nlist、dict、set、自定义类实例\n\n\n不可变对象的「值」一旦创建就无法修改，所谓 “修改” 其实是变量指向了新的对象；\n元组是特殊的不可变对象：若嵌套了可变对象（如列表），可变子对象的值可改，但元组本身的元素（子对象的引用）不可改。\n直接赋值&#x2F;浅拷贝&#x2F;深拷贝不可变对象赋值后指向原地址，但赋值后变量修改 &#x3D; 创建新对象，不影响原变量。\n所以以下我们讨论的是不可变对象的问题。\n\n\n\n类型\n定义\n特点\n\n\n\n直接赋值\n仅传递对象的「引用」（相当于给对象起了一个新名字）\n原对象和新变量指向同一个内存地址，修改一个会影响另一个\n\n\n浅拷贝\n创建新对象，但新对象的「子元素」仍引用原对象的子元素（只拷贝第一层）\n修改新对象的「第一层元素」不影响原对象；修改「子元素」会影响原对象\n\n\n深拷贝\n创建新对象，且递归拷贝所有子元素（所有层级都拷贝）\n原对象和新对象完全独立，修改任何层级的元素都互不影响\n\n\n难题不会的看 代码随想录\n148. 排序链表-Hard从底至顶的归并排序——动画演示：单链表排序      \n# Definition for singly-linked list.# class ListNode:#     def __init__(self, val=0, next=None):#         self.val = val#         self.next = nextclass Solution:    def sortList(self, head: Optional[ListNode]) -&gt; Optional[ListNode]:        # bad case 空链表或单节点链表        if not head or not head.next:            return head                # 计算链表长度        tmp = head        n = 0        while tmp:            n += 1            tmp = tmp.next        # 创建虚拟头节点        dummy_head = ListNode(-1)        dummy_head.next = head        # 开始归并排序        sub_size = 1        while sub_size &lt; n:                        prev = dummy_head # 已合并的节点的尾部指针            current = dummy_head.next # 待合并的头部指针            while current:                # 截取第一段有序链表                h1, current = self.splitList(current, sub_size)                # 截取第二段有序链表                h2, current = self.splitList(current, sub_size)                # 合并有序链表                mer_head, mer_tail = self.mergeTwoList(h1, h2)                # 接回去                prev.next = mer_head                # 准备下一轮归并                prev = mer_tail                        sub_size *= 2        return dummy_head.next    def splitList(self, head, n) -&gt; list[Optional[ListNode], Optional[ListNode]]:        &quot;&quot;&quot;        辅助函数-从head开始的n的节点链表        return: 截取的子链表头，剩余链表的头        &quot;&quot;&quot;        if not head:            return head, None                # 截取链表的尾指针        prev = None        # 找的是剩余链表的头        curr = head        count = 0        while curr and count &lt; n:            prev = curr            curr = curr.next            count += 1        # 必须截断尾指针的next        if prev:            prev.next = None                return head, curr    def mergeTwoList(self, l1, l2) -&gt; list[Optional[ListNode], Optional[ListNode]]:        &quot;&quot;&quot;        辅助函数-合并两个有序链表        &quot;&quot;&quot;        # 虚拟头节点，后续接收有序链表        dummy_head = ListNode(0)        curr = dummy_head        while l1 and l2:            if l1.val &lt;= l2.val:                curr.next = l1                l1 = l1.next            else:                curr.next = l2                l2 = l2.next            curr = curr.next                # 合并剩余节点        curr.next = l1 if l1 else l2        # 找到合并后链表的尾部        while curr.next:            curr = curr.next        return dummy_head.next, curr\n\n23. 合并 K 个升序链表递归&#x2F;归并排序\n# Definition for singly-linked list.# class ListNode:#     def __init__(self, val=0, next=None):#         self.val = val#         self.next = nextclass Solution:    def mergeKLists(self, lists: List[Optional[ListNode]]) -&gt; Optional[ListNode]:        if not lists: return         return self.merge(lists, 0, len(lists)-1)    def merge(self, lists, left, right):        # bad case        if left == right:            return lists[left]        mid = (left + right)//2        l1 = self.merge(lists, left, mid)        l2 = self.merge(lists, mid+1, right)        return self.mergeTwoList(l1, l2)    def mergeTwoList(self, l1, l2):        &quot;&quot;&quot;        合并有序链表递归版        &quot;&quot;&quot;        # bad case        if not l1: return l2        if not l2: return l1        if l1.val &lt; l2.val:            l1.next = self.mergeTwoList(l1.next, l2)            return l1        else:            l2.next = self.mergeTwoList(l1, l2.next)            return l2\n\n146. LRU 缓存class DLinkedNode:    def __init__(self, key: int=0, value: int=0):        self.key = key        self.value = value        self.prev = None        self.next = Noneclass LRUCache:    def __init__(self, capacity: int):        self.cache = dict()        # 初始化双向链表        # 虚拟头节点        self.head = DLinkedNode()        # 虚拟尾节点        self.tail = DLinkedNode()        self.head.next = self.tail        self.head.prev = self.head        self.capacity = capacity        self.size = 0            def get(self, key: int) -&gt; int:        if key not in self.cache:            return -1        node = self.cache[key]        self.moveToHead(node)        return node.value    def put(self, key: int, value: int) -&gt; None:        if key not in self.cache:            node = DLinkedNode(key, value)            self.cache[key] = node            self.addToHead(node)            self.size += 1            if self.size  &gt; self.capacity:                node = self.removeTail()                self.size -= 1                self.cache.pop(node.key)        else:            node = self.cache[key]            node.value = value            self.moveToHead(node)    def addToHead(self, node):        node.prev = self.head        node.next = self.head.next        self.head.next.prev = node        self.head.next = node    def removeNode(self, node):        node.prev.next = node.next        node.next.prev = node.prev    def moveToHead(self, node):        self.removeNode(node)        self.addToHead(node)    def removeTail(self) -&gt; Optional[DLinkedNode]:        node = self.tail.prev        self.removeNode(node)        return node # 必须返回移除的节点，用于清理cache# Your LRUCache object will be instantiated and called as such:# obj = LRUCache(capacity)# param_1 = obj.get(key)# obj.put(key,value)\n\n\n\nACM模式套用核心代码\nimport sysclass Solution:    def add(self, a: int, b: int) -&gt; int:        return a + b# 读取到 EOFfor line in sys.stdin:    a, b = map(int, line.strip().split())    result = Solution().add(a, b)    print(result)\n\n基础输入 - input()line = input()# 输入: hello world# line = &quot;hello world&quot;n = int(input())# 输入: 123# n = 123f = float(input())# 输入: 3.14# f = 3.14parts = input().split()# 输入: apple banana cherry# parts = [&quot;apple&quot;, &quot;banana&quot;, &quot;cherry&quot;]numbers = list(map(int, input().split()))# 输入: 1 2 3 4 5# numbers = [1, 2, 3, 4, 5]\n\n多行输入\n# 例：第一行是 N，第二行是 N 个整数n = int(input())numbers = list(map(int, input().split()))# 例：处理多组数据，直到读到 EOF (End Of File)import sysfor line in sys.stdin:    # line = line.strip() # 可以去掉行末的换行符    data = list(map(int, line.split()))    # 处理 data...\n\n基础输出 - print()a, b = 10, 20# 使用 f-string (推荐)print(f&quot;The sum of &#123;a&#125; and &#123;b&#125; is &#123;a+b&#125;.&quot;)# 输出: The sum of 10 and 20 is 30.# 使用 .format()print(&quot;The sum of &#123;&#125; and &#123;&#125; is &#123;&#125;.&quot;.format(a, b, a+b))# 输出: The sum of 10 and 20 is 30.numbers = [1, 2, 3, 4, 5]print(*numbers)# 输出: 1 2 3 4 5numbers = [1, 2, 3, 4, 5]print(&#x27; &#x27;.join(map(str, numbers)))# 输出: 1 2 3 4 5\n\n构建链表nums = list(map(int, input().strip().split(&#x27;,&#x27;)))class ListNode:    def __init__(self, val=0, next=None):        self.next = next        self.val = valhead = ListNode()curr = headfor num in nums:    curr.next = ListNode(num)    curr = curr.nextcurr = headwhile curr:    print(curr.val)    curr = curr.next\n\n","categories":["LeetCode"],"tags":["LeetCode","刷题笔记"]},{"title":"Hexo/Butterfly 常用教程","url":"/2026/04/18/Hexo/","content":"Welcome to Hexo! This is your very first post. Check documentation for more info. If you get any problems when using Hexo, you can find the answer in troubleshooting or you can ask me on GitHub.\n快速开始创建新文章$ hexo new [layout] &lt;title&gt;\n\nMore info: Writing\n清理静态文件$ hexo clean$ # 缩写$ hexo cl\n\n生成静态文件$ hexo generate$ # 缩写$ hexo g\n\nMore info: Generating\n本地运行服务器$ hexo server$ # 缩写$ hexo s\n\nMore info: Server\n远程部署$ hexo deploy$ # 缩写$ hexo d\n\nMore info: Deployment\n"}]